%li{:id => dom_id(movie)}
  = link_to movie.title, movie, :class => 'colorbox-link'
  %span{ :class => "to" }
    = link_to image_tag("cancel.png"), movie, :confirm => 'Are you sure you want to remove this movie?', :method => :delete

