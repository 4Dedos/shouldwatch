<%= page_replace_html('recommendation_link',{:partial => 'recommendation_link', :object => @recommendation }) %>
$(".movieContainer .recommendation_link").slideUp().effect("highlight", {}, 3000);
$.colorbox.resize();
$('input#recommendation_link').focus();